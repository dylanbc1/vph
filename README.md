# Control VPH 🚨 — Chat tipo WhatsApp

UI móvil que simula una conversación de WhatsApp del grupo **Control VPH**, con participantes: VPH, Sistema Inmune, Célula Cervical, Papanicolaou, Prueba VPH y Vacuna. Solo frontend (React + Vite + Tailwind).

## Cómo correr el proyecto

```bash
npm install
npm run dev
```

Abre la URL que muestre Vite (por ejemplo `http://localhost:5173`) en el navegador. En pantallas grandes se ve como un teléfono centrado; en móvil ocupa todo el ancho.

## Scripts

- `npm run dev` — servidor de desarrollo
- `npm run build` — build de producción
- `npm run preview` — vista previa del build
